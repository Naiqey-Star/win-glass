# v1.1.0 — 托盘右键菜单新增两个分段式透明度滑块

> **一句话**：现在可以**直接在托盘右键菜单里**调透明度了 —— 分段式滑块、步进 1%、整数取值、改完自动记住。

让当前正在用的窗口保持清晰、其余窗口自动变半透明的小工具。常驻系统托盘，退出时全部还原。

---

## 📦 下载

| 文件 | 大小 | 说明 |
| --- | --- | --- |
| `win_glass_setup_v1.1.0.exe` | 29.8 MB | **安装包**（双击即装，**不需要管理员权限**，不弹 UAC） |
| `win_glass_setup_v1.1.0.exe.sha256` | 100 B | 上述文件的 SHA256 校验值 |

包内已含运行时，**目标机器不需要安装 Python**。

```
c9071a59e448aeed4075cd1a226a51ad1bc9d037aa68036ef1361fc2fc1c6ade  win_glass_setup_v1.1.0.exe
```

### 校验下载文件

```bash
# Linux / macOS
sha256sum -c win_glass_setup_v1.1.0.exe.sha256
```

```powershell
# Windows PowerShell
Get-FileHash .\win_glass_setup_v1.1.0.exe -Algorithm SHA256
```

---

## 🚀 安装与使用

1. 双击 `win_glass_setup_v1.1.0.exe`
   > 安装包本身是命令行程序，会有一个黑窗口显示安装进度，属正常现象。
   > 从 v1.0.1 升级安装**直接覆盖即可**，配置文件里已有的滑块取值会保留。
2. 开始菜单搜索 **win_glass** 打开 → **不会再有任何命令行窗口**
3. 右下角托盘出现图标即已在工作

| 托盘操作 | 效果 |
| --- | --- |
| 左键单击 | 暂停 / 继续（暂停时所有窗口立刻恢复 100%） |
| 右键 | 暂停、立即恢复、**两个透明度滑块**、打开日志、退出 |

### 右键菜单里的两个滑块

```
┌──────────────────────────────┐
│ 暂停（所有窗口恢复 100%）        │
│ 立即把所有窗口恢复 100%          │
├──────────────────────────────┤
│ 非聚焦最低透明度          40%  │
│ ▮▮▮▮▮▮▮▮▮▮▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯▯ │
│ 聚焦最高透明度           100%  │
│ ▮▮▮▮▮▮▮▮▮▮▮▮▮▮▮▮▮▮▮▮▮▮▮▮▮▮▮▮ │
├──────────────────────────────┤
│ 打开日志                       │
│ 退出（还原全部窗口）             │
└──────────────────────────────┘
```

| 滑块 | 作用 | 范围 | 默认 |
| --- | --- | --- | --- |
| **非聚焦最低透明度** | 没在用的窗口淡化到多少 | **5% ~ 95%** | 40% |
| **聚焦最高透明度** | 正在用的窗口清晰到多少 | **5% ~ 100%** | 100% |

- **分段式显示**：一格 = 1%，点亮几格就是百分之几，一眼能数出来
- **步进恒为 1%**，数值始终是**整数**，不会出现小数点
- **三种调法**：按住拖动 / 鼠标滚轮（±1%）/ 左右方向键（±1%）
- 拖动时菜单**不会关闭**，松手后可以接着点别的项
- **改完自动记住**：写入 `%LOCALAPPDATA%\win_glass\config.json`，下次开机照旧

- 安装位置：`%LOCALAPPDATA%\Programs\win_glass`（用户目录，不需要管理员）
- 开机自启：`win_glass_setup_v1.1.0.exe --autostart`（取消用 `--no-autostart`）
- 卸载：控制面板「程序和功能」→ win_glass，或安装目录下的 `uninstall.exe`

---

## ✨ 本版变更

### 新增

- **托盘右键菜单里的两个分段式滑块**（见上表），把最常改的两个参数从命令行搬到了菜单里
- **取值持久化**：两个滑块的值写入配置文件，下次启动自动生效。
  写盘做了 0.3s 节流，拖动时不会疯狂刷盘；关菜单 / 退出时强制落盘
- 新参数 `--active-alpha`（聚焦窗口透明度）、`--no-config`（不读不写配置，改动只在本次运行有效）、
  `--save-config`（把本次参数写入配置后继续运行）。优先级：**命令行 > 配置文件 > 内置默认**

### 修复

- **滑块尺寸不生效、菜单项一片空白（本版核心根因）**
  `WM_MEASUREITEM` 与 `WM_DRAWITEM` 的消息号写反了
  （正确是 `WM_DRAWITEM=0x2B` / `WM_MEASUREITEM=0x2C`）。
  写反**不会报任何错、消息也照样收得到**，只是拿到的是**另一个结构体**：
  宽高被写进 `DRAWITEMSTRUCT` 的 `itemAction/itemState`，绘制时读到的 `rcItem`
  又是 `MEASUREITEMSTRUCT` 里的残留值 → 表现为「怎么设都是默认的 203×16、整项空白」。
- **滚轮读了错误字段**：滚轮增量在 `lParam` 高 16 位，原先从 `wParam` 高位取，
  取到的是光标 y 坐标 → 滚轮乱跳或完全不动
- **`--no-config` 启动即崩**：空配置 + 无参数时 `apply_saved` 返回 `(None, None)`，
  构造函数直接 `TypeError`。**新机器首次运行也会崩**，因为那时还没有 config.json
- **`MFT_OWNERDRAW` 常量用错值会静默失败**（必须 `0x00000100`），现在失败会打印明确警告
- 诊断用的消息明细改为最多保留 64 条，常驻进程不会越跑越占内存

---

## ✅ 验证结果

| 测试 | 内容 | 结果 |
| --- | --- | --- |
| `slider_test.py` | 取值量化与夹紧 / x→百分比映射（1% 步进、两端可达）/ **真画一遍再用 `GetPixel` 反查填充边界** / 配置往返与节流 / 滚轮与点击的字段解析 / 空配置与 `--no-config` 起得来 | **87 PASS / 0 FAIL** |
| `menu_e2e_test.py` | 真弹菜单并截图 / 合成鼠标拖动（菜单不关、值实时变）/ 方向键微调 / 落盘整数 / 点外部正常关闭 | **18 PASS / 0 FAIL** |
| `robustness_test.py` | 参数边界 / 零窗口 / 30s 长跑内存与 CPU / 强杀还原 / 托盘 / 优雅退出 | **27 PASS / 0 FAIL** |
| `e2e_test.py` | 真实窗口聚焦 ↔ 失焦切换，实测透明度 100% ↔ 40% | **PASS** |
| `install_test.py` | 沙盒完整走 安装 → 校验 → 卸载 → 清理（用本版安装包实测） | **25 PASS / 0 FAIL** |

关键证据（`menu_e2e_test.py` 实测的菜单项矩形，期望高度 42）：

```
('measure', 1, 10, 0, 16)       ← 系统问尺寸
('measure-set', 0, 10, 300, 42) ← 我们回 300x42
('draw', 1, 10, 338, 42)        ← 实际绘制矩形高度精确 42
```

> 排障与实现细节（含「消息常量写反」的完整定位过程、可复用的四步手法）见
> [`docs/win_glass_v1.1.0_slider_report.md`](docs/win_glass_v1.1.0_slider_report.md)。

---

## 📋 环境要求

| 项 | 要求 |
| --- | --- |
| 系统 | Windows 10 / 11（**x64**） |
| Python | **不需要**，安装包已含运行时 |
| 管理员权限 | **不需要**（装到用户目录） |
| 其他依赖 | 无（程序只用 Windows 自带接口，纯 `ctypes`） |

---

## ⚠️ 已知限制

- 只支持 Windows；核心依赖 `WS_EX_LAYERED`，其他系统没有对应机制
- 部分**独占全屏**或**硬件加速自绘**的程序（某些游戏、播放器）不响应整窗透明度，属系统限制
- 默认**跳过全屏窗口**，避免看电影 / 玩游戏时被干扰（`--no-skip-fullscreen` 可关闭此行为）
- 部分 **Electron / Chromium** 应用自带分层透明度，默认会**记录其原值并在退出时还原**；
  也可用 `--skip-foreign-layered` 直接跳过
- 菜单里滑块的**宽度**由系统在请求值上再加一段菜单留白（本机实测 300 → 338px），
  高度是精确的；绘制与命中判定都按实际矩形计算，不受影响
- **第三方外壳增强工具**（StartAllBack、ExplorerPatcher、Windhawk、Winstep 等）会挂钩菜单代码，
  理论上可能干扰自绘菜单项
- 内置排除清单为经验值，遇到特殊软件可用 `--exclude <类名>` 补充

---

## 🔧 常用参数

```bat
:: 想先看看它会接管哪些窗口（不做任何修改）
win_glass-console.exe --list

:: 背景想更淡一点
win_glass-console.exe --inactive-alpha 0.25

:: 过渡想更快
win_glass-console.exe --fade-ms 200

:: 排除某个软件的窗口
win_glass-console.exe --exclude MyAppClass,AnotherClass

:: 临时试一下、不留记录（菜单里拖滑块也只对本次运行有效）
win_glass-console.exe --no-config --inactive-alpha 0.25

:: 把这次的设置固化成默认值
win_glass-console.exe --save-config --inactive-alpha 0.25 --active-alpha 1.0
```

完整参数表、工作原理与常见问题见仓库 [README.md](README.md)。

---

## 📄 许可

**本项目未附开源许可证（All rights reserved）**，代码公开可见但不授予复制 / 修改 / 再分发 / 商用权利。
如需以开源许可证发布或授权使用，请通过 issue 联系作者。

---
---

# 历史版本

以下为旧版本的发行说明（保留存档）。

## v1.0.1 — 修复安装后不可用 · 改为无窗口 + 系统托盘

> **一句话**：这一版让 win-glass **装上就能用** —— 不再有命令行黑窗口，也不再依赖 `.vbs` 启动器。

让当前正在用的窗口保持清晰、其余窗口自动变半透明的小工具。常驻系统托盘，退出时全部还原。

---

### 📦 下载

| 文件 | 大小 | 说明 |
| --- | --- | --- |
| `win_glass_setup_v1.0.1.exe` | 30 MB | **安装包**（双击即装，**不需要管理员权限**，不弹 UAC） |
| `win_glass_setup_v1.0.1.exe.sha256` | 100 B | 上述文件的 SHA256 校验值 |

```
cf036ab050f7d570e82bd815f836dcb6f34b242ffe0afd810798326b06a83e54  win_glass_setup_v1.0.1.exe
```

### 🚀 安装与使用

1. 双击 `win_glass_setup_v1.0.1.exe`
2. 开始菜单搜索 **win_glass** 打开 → **不会再有任何命令行窗口**
3. 右下角托盘出现图标即已在工作

| 托盘操作 | 效果 |
| --- | --- |
| 左键单击 | 暂停 / 继续（暂停时所有窗口立刻恢复 100%） |
| 右键 | 暂停、立即恢复、打开日志、退出 |

### ✨ 该版变更

- **修复**：快捷方式弹出记事本（`.vbs` 启动器被改关联）
- **修复**：启动弹命令行黑窗口（改为 `--windowed` 无窗口）
- **修复**：托盘「退出」不还原窗口
- **修复**：卸载时收不到退出信号（托盘窗口改为隐藏顶层窗口）
- **修复**：`--log` 参数失效
- **新增**：系统托盘交互、随包附带 `win_glass-console.exe`、日志文件

### ✅ 该版验证

| 测试 | 内容 | 结果 |
| --- | --- | --- |
| `robustness_test.py` | 参数边界 / 零窗口 / 30s 长跑 / 强杀还原 / 托盘 / 优雅退出 | 27 PASS / 0 FAIL |
| `e2e_test.py` | 真实窗口聚焦 ↔ 失焦切换，实测透明度 100% ↔ 40% | PASS |
| `install_test.py` | 沙盒完整走 安装 → 校验 → 卸载 → 清理 | PASS |

---

## v1.0.0 — 首个版本

- 窗口聚焦透明度引擎：有焦点 / 置顶窗口 100%，其余 40%
- 500ms smoothstep 缓动过渡，不会生硬跳变
- 纯 `ctypes` 实现，不依赖 `pywin32`
- 窗口枚举与过滤（桌面、任务栏、输入法、提示框、全屏窗口等）
- 退出时还原透明度与窗口扩展样式
- 命令行参数支持，附端到端与健壮性测试
