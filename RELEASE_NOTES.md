# v1.6.0 — 层叠衰减：未聚焦窗口按堆叠顺序逐层递减

> **一句话**：以前 4 个未聚焦窗口都是同一个透明度，彼此**没有任何层次**；
> 现在它们会按 **Windows 的堆叠顺序（Z 序）从上到下逐层递减** ——
> 最上面那个未聚焦窗口用「非聚焦最低透明度」，往下每层乘一次「层衰减系数」（默认 0.70，下限 5%）。

让当前正在用的窗口保持清晰、其余窗口自动变半透明的小工具。常驻系统托盘，退出时全部还原。

---

## 📦 下载

| 文件 | 大小 | 说明 |
| --- | --- | --- |
| `win_glass_setup_v1.6.0.exe` | 29.8 MB | **安装包**（双击即装，**不需要管理员权限**，不弹 UAC） |
| `win_glass_setup_v1.6.0.exe.sha256` | 94 B | 上述文件的 SHA256 校验值 |

包内已含运行时，**目标机器不需要安装 Python**。

```
957f9f7755a82d351c5ed9e058f05db19385b9268ee27bb29a836940cc01821a  win_glass_setup_v1.6.0.exe
```

### 校验下载文件

```bash
# Linux / macOS
sha256sum -c win_glass_setup_v1.6.0.exe.sha256
```
```powershell
# Windows PowerShell
(Get-FileHash .\win_glass_setup_v1.6.0.exe -Algorithm SHA256).Hash.ToLower()
```

---

## 🚀 安装与使用

1. 双击 `win_glass_setup_v1.6.0.exe`
   > 安装包本身是命令行程序，会有一个黑窗口显示安装进度，属正常现象。
   > 从旧版本升级安装**直接覆盖即可**，配置文件里已有的取值会保留。
2. 开始菜单搜索 **win_glass** 打开 → **不会再有任何命令行窗口**
3. 右下角托盘出现图标即已在工作

| 托盘操作 | 效果 |
| --- | --- |
| 左键单击 | 暂停 / 继续（暂停时所有窗口立刻恢复 100%） |
| 右键 | 暂停、立即恢复、悬停开关、全屏开关、**层叠衰减开关**、**四个滑块**、**渐隐时间**、打开日志、退出 |

### 右键菜单长这样

```
┌──────────────────────────────────┐
│ 暂停（所有窗口恢复 100%）            │
│ 立即把所有窗口恢复 100%              │
│ 悬停半透明（未聚焦窗口 → 88%）        │
│ 全屏窗口固定 100%                   │
│ 层叠衰减（第 1~4 层 40/28/20/14%）   │  ← 本版新增：点一下整体开/关
├──────────────────────────────────┤
│ 非聚焦最低透明度           40%   │
│ ●━━━━━━━━━━━━━━━○──────────── │  ← 细轨道 + 圆形手柄，悬停变大
│ 聚焦最高透明度            100%   │
│ ●━━━━━━━━━━━━━━━━━━━━━━━━━━○ │
│ 悬停插值系数               0.8   │
│ ●━━━━━━━━━━━━━━━━━━━━━○───── │
│ 层衰减系数                 0.7   │  ← 本版新增：0.1 ~ 1.0，步进 0.1
│ ●━━━━━━━━━━━━━○────────────── │
├──────────────────────────────────┤
│ 渐隐时间…                 500 ms │
├──────────────────────────────────┤
│ 打开日志                          │
│ 退出（还原全部窗口）                │
└──────────────────────────────────┘
```

### 「层叠衰减」怎么用

**解决的问题**：桌面上开着 5 个窗口，其中 4 个未聚焦。以前这 4 个全是同一个透明度（比如 40%），
彼此**没有任何层次** —— 看不出谁压在谁上面，视觉上会糊成一片。

**做法**：把所有**普通未聚焦窗口**按 Windows 的 **Z 序**（最顶 → 最底）依次编号，
第 1 层用「非聚焦最低透明度」，之后每一层乘一次「层衰减系数」：

```
输入
  base   = 非聚焦最低透明度（滑块，默认 40%）
  ratio  = 层衰减系数（滑块，默认 0.70）
  floor  = 下限 5%（写入代码的常量）
  层级号 = 该窗口在所有「普通未聚焦窗口」里排第几（1 起）

输出（不透明度，整数百分比）
  第 1 层 = base
  第 n 层 = 四舍五入( 第 n−1 层已经显示出来的整数 × ratio )，但不低于 floor
```

以默认值 `base = 40%`、`ratio = 0.70` 为例：

| 层级 | 计算 | 结果 |
| --- | --- | --- |
| 第 1 层 | 起点 | **40%** |
| 第 2 层 | 40 × 0.7 = 28 | **28%** |
| 第 3 层 | 28 × 0.7 = 19.6 → 20 | **20%** |
| 第 4 层 | 20 × 0.7 = 14 | **14%** |
| 第 5 层 | 14 × 0.7 = 9.8 → 10 | **10%** |
| 第 6 层 | 10 × 0.7 = 7 | **7%** |
| 第 7 层及以下 | 7 × 0.7 = 4.9 → 但下限 5 | **5%** |

把「非聚焦最低透明度」滑块拖到 **50%**（「聚焦最高透明度」拖到 90%），
那么四个窗口就是：**聚焦 90% / 第 1 层 50% / 第 2 层 35% / 第 3 层 25%**。

拖动「层衰减系数」，菜单上面那行「**层叠衰减（第 1~4 层 xx/xx/xx/xx%）**」会
**实时跟着变**，所见即所得。

> 点一下菜单里那一项就能**整体开关**层叠衰减；关掉后所有未聚焦窗口统一用最低值
> （即 v1.5.0 及以前的行为）。命令行对应 `--no-layer-decay` / `--layer-decay-ratio`。
> 改完自动写进 `%LOCALAPPDATA%\win_glass\config.json`（键名 `layer_decay` / `layer_decay_ratio`）。

### 三层递推，为什么这么算

1. **四舍五入就是四舍五入**，不是编程语言默认的"银行家舍入"。
   Python 内建 `round(24.5)` 会给 `24`，而这里必须是 `25`，所以自己实现了 `round_half_up()`。
2. **乘的是"上一层已经取整显示的值"**，而不是 `base × ratio^(n−1)`。
   好处是你**在屏幕上看到 28% 就能心算出下一层是 19.6 → 20%**，每一层都能肉眼验证。
   代价是舍入误差顺层累积 —— 这是**有意**的取舍：链内自洽、肉眼可验，
   比"绝对精确但与屏幕对不上"更符合这个工具的定位。
3. **只有"普通未聚焦窗口"参与编号。**
   全屏 / 聚焦 / 最大化 / 置顶**都不占层级号**、也不参与递减（它们走各自更高的优先级）。
   尤其是**置顶窗口**：它物理上位置很靠上，若让它占号，它下面所有窗口会被整体推低一层，
   而它自己又不参与递减 —— 层级就完全无法解释了。一句话：
   **"视同聚焦"的窗口既不被层叠压暗，也不影响别人的层号。**

### 层级一变，结果怎么更新

引擎每轮重算（默认 **0.15 秒**一次）都完整走三趟：

```
第 1 趟  逐个窗口判定状态（全屏 / 聚焦 / 最大化 / 置顶），
         顺手收集出「普通未聚焦窗口」这个集合
第 2 趟  读一次 Z 序链（GetTopWindow → GW_HWNDNEXT），
         给第 1 趟收集到的集合按 Z 序编号 → {窗口: 层级}
第 3 趟  逐个窗口按「层级号」算出目标值，交给缓动动画
```

必须分三趟、不能一趟算完，是因为**先算的窗口不知道后面还有几个普通未聚焦窗口排在它下面**。
三趟全过程都是**只读**的，不写任何 alpha —— 真正把透明度写到屏幕上的只有动画帧。
所以切窗口、最小化、新建窗口导致 Z 序一变，下一轮（≤ 0.15s）就会**整链重算**并平滑过渡。

在 `--list` 体检里可以直接看到每个窗口排第几层：

```
> win_glass-console.exe --list
可管理窗口 3 个   未聚焦目标=50%   聚焦/最大化/置顶目标=90%   全屏固定=100%   渐隐=500ms
判定优先级: 全屏(恒 100%，不受最高值设置影响) > 聚焦 > 最大化 > 置顶 > 悬停 > 普通非聚焦(层叠衰减)
标记 F=全屏 Z=最大化 T=置顶   层=层叠层级(1 起，0=不参与层叠)
层叠衰减：开   第 1~5 层 = 50% / 35% / 25% / 18% / 13%（系数 0.70，下限 5%）
      HWND   目标    层  原因     FZT  ...
 0x000A1234    90%     0  聚焦     ---  ...
 0x000B5678    50%     1  第1层    ---  ...
 0x000C9ABC    35%     2  第2层    ---  ...
```

---

## ✨ 本版变更

### 新增

- **层叠衰减**：普通未聚焦窗口按 Z 序逐层递减（规则见上）。
- **第四个滑块「层衰减系数」**：范围 **0.1 ~ 1.0**、步进 **0.1**、显示**一位小数**，
  与悬停系数滑块复用同一套「整数档位 × 0.1」机制，不引入浮点步进误差。
- **菜单里的「层叠衰减」开关项**：菜单上直接显示第 1~4 层的当前计算值，点一下整体开/关。
- 命令行参数 `--no-layer-decay`（关）与 `--layer-decay-ratio <0.1~1.0>`。
- `--list` 体检新增**「层」列**（`0` = 不参与层叠）与「层叠衰减：开　第 1~5 层 = …」行。
- 纯函数 `round_half_up()` / `layer_pct()` / `z_order_windows()` / `assign_depths()`，
  单测与 `--list` 复用同一份逻辑，避免两处各写一套早晚不一致。

### 变更

- 配置文件新增两个键：`layer_decay`（布尔，默认 `true`）、
  `layer_decay_ratio`（浮点，默认 `0.70`）。**旧配置直接沿用，无需迁移。**
- 判定优先级链末端细化为「普通未聚焦 → 层叠衰减」，不再是单一值。
- 安装包可执行文件的**作者信息**更新为 `Naiqey.千鵺`（此前版本残留旧的作者名）。

---

## ✅ 验证结果

| 测试 | 内容 | 结果 |
| --- | --- | --- |
| `slider_test.py` | 取值量化与夹紧 / x→档位映射 / **离屏真画并用 `GetPixel` 反查** / 配置往返 / 滚轮与点击字段 / 数值输入框 / 菜单项逐项核对 / 悬停系数双向换算 / 最大化≠全屏回归 / **层叠衰减全套（递推、四舍五入、封底、Z 序编号、优先级不被抢、CLI）** | **302 PASS / 0 FAIL** |
| `hover_live_test.py` | **真实桌面 + 真实光标**上的悬停判定与状态判定链，并证明整轮**一个写接口都不调** | **19 PASS / 0 FAIL / 2 SKIP** |
| `menu_e2e_test.py` | 真弹托盘菜单 + 合成鼠标拖动 + 截图，断言滑块数量/尺寸/落盘值 | **20 PASS / 0 FAIL** |
| `menu_live_test.py` | 拖动时**数值文字与进度条真的在重绘**（`BitBlt` 抓像素差异） | **7 PASS / 0 FAIL** |
| `robustness_test.py` | 参数边界 / 零窗口 / 30s 长跑内存与 CPU / 强杀还原 / 托盘 / 优雅退出 | **27 PASS / 0 FAIL** |
| `e2e_test.py` | 真实窗口聚焦 ↔ 失焦切换，实测透明度 100% ↔ 40% | **PASS** |
| `install_test.py` | 沙盒完整走 安装 → 校验 → 卸载 → 清理（用本版安装包实测） | **25 PASS / 0 FAIL** |

本版关键实测（`slider_test.py` 新增断言）：

```
round_half_up(24.5) = 25     round_half_up(17.5) = 18     round_half_up(2.5) = 3
   ← 内建 round() 会给 24 / 18 / 2，所以必须自己实现

base=50% ratio=0.70  →  50 / 35 / 25 / 18 / 13 / 9 / 6 / 5 / 5 …   （第 3 层 24.5→25，第 4 层 17.5→18）
用户原例（聚焦 90%） →  90 / 50 / 35 / 25                      ← 四个窗口
ratio=0.1 连续 20 层 →  最终停在 5%（下限），永远不会到 0

Z 序 [置顶A, 普通B, 聚焦C, 普通D, 最大化E]，普通集合={B,D}
  →  B=第1层  D=第2层        （置顶/聚焦/最大化不占号）
反转 Z 序 →  D=第1层  B=第2层 （层级完全由 Z 序决定）

置顶窗口带 depth=3 → 仍走 90%「置顶」，不被层叠压暗
最大化窗口带 depth=3 → 仍走 90%「最大化」
全屏窗口带 depth=3 → 仍是恒定 100%
```

三个 exe 的版本资源均确认为 `1.6.0.0`，`CompanyName` 为 `Naiqey.千鵺`。

---

## 🧭 在此之前还补上了什么（v1.3.0 / v1.4.0 / v1.4.1 / v1.5.0）

| 版本 | 内容 |
| --- | --- |
| **v1.3.0** | 新增**「悬停半透明」**：鼠标压住的非焦点窗口平滑提亮。只提亮、绝不压暗 —— 全屏/聚焦/最大化/置顶一律不参与。改用 50ms 轮询 `GetCursorPos` + `WindowFromPoint`，**光标没动就直接短路** |
| **v1.4.0** | **最大化 / 置顶窗口视作聚焦**（跟随「聚焦最高透明度」）；**全屏窗口恒定 100%**（独立常量）。新增托盘开关「全屏窗口固定 100%」，`--list` 增加 `F` / `Z` / `T` 标记列 |
| **v1.4.1** | **修复「一切最大化就变 100%」**：根因是 Windows 最大化时会把窗口矩形**撑到屏幕外面**（实测 `(-8,-8,1928,1088)` 对 `(0,0,1920,1080)`）—— 只按"矩形是否盖住屏幕"判，**所有最大化窗口都会被误判成全屏**。修法是 `IsZoomed()` **一票否决** + 矩形判定留 2px 内缩容差 |
| **v1.5.0** | **「悬停插值系数」改为可调滑块**（0.0 ~ 1.0，步进 0.1）：悬停值由写死系数改为 `最低 + (最高 − 最低) × 滑块值`，默认从 0.5 调整为 0.8 |

判定优先级链现在是：

```
全屏（恒 100%，不受「聚焦最高透明度」影响）
  └─ 聚焦（前台窗口）           → 聚焦最高透明度
      └─ 最大化 IsZoomed()      → 聚焦最高透明度
          └─ 置顶 TOPMOST       → 聚焦最高透明度
              └─ 悬停（鼠标压住） → 最低 + (最高 − 最低) × 滑块的系数
                  └─ 普通未聚焦   → 层叠衰减：第 1 层 = 最低值，
                                    之后每层 = 上一层取整显示值 × 层衰减系数（下限 5%）
```

---

## 📋 环境要求

| 项 | 要求 |
| --- | --- |
| 系统 | Windows 10 / 11（**x64**） |
| Python | **不需要**，安装包已含运行时 |
| 管理员权限 | **不需要**（装到用户目录） |
| 其他依赖 | 无（程序只用 Windows 自带接口，纯 `ctypes`） |

---

## ⚠️ 已知限制

- 只支持 Windows；核心依赖 `WS_EX_LAYERED`，其他系统没有对应机制
- 部分**独占全屏**或**硬件加速自绘**的程序（某些游戏、播放器）不响应整窗透明度，属系统限制
- **全屏窗口默认接管并锁在 100%**；若某些全屏程序不喜欢被接管，用 `--skip-fullscreen`
  让它们完全不参与（程序会原样保留其透明度与扩展样式）
- 部分 **Electron / Chromium** 应用自带分层透明度，默认会**记录其原值并在退出时还原**；
  也可用 `--skip-foreign-layered` 直接跳过它们
- **层叠层级用的是"顶层窗口"的 Z 序**：如果某个应用内部有多个独立顶层窗口，
  它们会被当作彼此独立的层（这是符合直觉的，因为它们在屏幕上确实互相遮挡）
- 若在用 **StartAllBack / ExplorerPatcher / Windhawk** 等外壳增强工具，它们会挂钩菜单代码，
  可能干扰自绘菜单项（滑块拖不动时可先在干净系统上验证）
- 装到用户目录，因此**只对当前用户生效**；多用户环境需各自安装
