# v1.5.0 — 悬停插值系数改为可调滑块（0.0 ~ 1.0，步进 0.1）

> **一句话**：托盘菜单新增**第三个滑块「悬停插值系数」**，鼠标压住的未聚焦窗口提亮到哪一档
> 现在由你**实时拖**决定；悬停值的计算从写死系数改为 `最低 + (最高 − 最低) × 滑块值`。

让当前正在用的窗口保持清晰、其余窗口自动变半透明的小工具。常驻系统托盘，退出时全部还原。

---

## 📦 下载

| 文件 | 大小 | 说明 |
| --- | --- | --- |
| `win_glass_setup_v1.5.0.exe` | 29.8 MB | **安装包**（双击即装，**不需要管理员权限**，不弹 UAC） |
| `win_glass_setup_v1.5.0.exe.sha256` | 94 B | 上述文件的 SHA256 校验值 |

包内已含运行时，**目标机器不需要安装 Python**。

```
6205d449a364738fde02233cfd764dec912e4193487b3e6ed5851544b73f3cc9  win_glass_setup_v1.5.0.exe
```

### 校验下载文件

```bash
# Linux / macOS
sha256sum -c win_glass_setup_v1.5.0.exe.sha256
```
```powershell
# Windows PowerShell
(Get-FileHash .\win_glass_setup_v1.5.0.exe -Algorithm SHA256).Hash.ToLower()
```

---

## 🚀 安装与使用

1. 双击 `win_glass_setup_v1.5.0.exe`
   > 安装包本身是命令行程序，会有一个黑窗口显示安装进度，属正常现象。
   > 从旧版本升级安装**直接覆盖即可**，配置文件里已有的取值会保留。
2. 开始菜单搜索 **win_glass** 打开 → **不会再有任何命令行窗口**
3. 右下角托盘出现图标即已在工作

| 托盘操作 | 效果 |
| --- | --- |
| 左键单击 | 暂停 / 继续（暂停时所有窗口立刻恢复 100%） |
| 右键 | 暂停、立即恢复、悬停开关、全屏开关、**三个滑块**、**渐隐时间**、打开日志、退出 |

### 右键菜单长这样

```
┌──────────────────────────────────┐
│ 暂停（所有窗口恢复 100%）            │
│ 立即把所有窗口恢复 100%              │
│ 悬停半透明（未聚焦窗口 → 88%）        │
│ 全屏窗口固定 100%                   │
├──────────────────────────────────┤
│ 非聚焦最低透明度           40%   │
│ ●━━━━━━━━━━━━━━━○──────────── │  ← 细轨道 + 圆形手柄，悬停变大
│ 聚焦最高透明度            100%   │
│ ●━━━━━━━━━━━━━━━━━━━━━━━━━━○ │
│ 悬停插值系数               0.8   │  ← 本版新增：0.0 ~ 1.0，步进 0.1
│ ●━━━━━━━━━━━━━━━━━━━━━○───── │
├──────────────────────────────────┤
│ 渐隐时间…                 500 ms │
├──────────────────────────────────┤
│ 打开日志                          │
│ 退出（还原全部窗口）                │
└──────────────────────────────────┘
```

### 「悬停插值系数」怎么用

鼠标停在**没有焦点**的窗口上时，那个窗口会从「非聚焦最低透明度」提亮一下。
提亮多少，由这个滑块决定：

```
悬停值 = 透明度最小值 + (最大值 − 最小值) × 滑块值
```

| 滑块值 | 效果（以 最低 40% / 最高 100% 为例） |
| --- | --- |
| `0.0` | 悬停值 = 40% —— 视觉上**等于没开悬停** |
| `0.5` | 悬停值 = 70% —— 正好取中间（v1.4.1 及以前的固定行为） |
| `0.8` | 悬停值 = 88% —— **本版默认**，偏向"更接近清晰"那一侧 |
| `1.0` | 悬停值 = 100% —— 悬停即完全清晰 |

拖动它，菜单上面那行「**悬停半透明（未聚焦窗口 → xx%）**」会**实时跟着变**，所见即所得。

> 两个透明度滑块步进 **1%**、显示整数；这个滑块步进 **0.1**、显示一位小数。
> 三种调法都一样：**按住拖动** / **鼠标滚轮** / **左右方向键**。
> 改完自动写进 `%LOCALAPPDATA%\win_glass\config.json`（键名 `hover_ratio`），下次开机照旧。

### 「渐隐时间…」怎么用

点开是一个小输入框，直接填**毫秒数**（**1 ~ 5000 ms**，默认 500）。改完**立即生效** ——
正在跑的过渡会按新时长重排时间轴，不用等下一次切换。越界自动夹紧，填非数字退回原值。

> 注意：滑块的改动会**立刻**作用到桌面窗口上——这是设计如此，所见即所得。
> 如果不希望留下记录，用 `--no-config` 启动，改动就只在本次运行有效。

---

## ✨ 本版变更

### 新增

- **第三个滑块「悬停插值系数」**：范围 **0.0 ~ 1.0**、**步进 0.1**、显示**一位小数**。
- 悬停透明度**不再用写死的系数**，改为跟随滑块实时计算（公式见上）。
- 新增显示格式化函数 `fmt_pct()` / `fmt_ratio_units()`；
  owner-draw 绘制函数 `draw_menu_slider()` 支持传入自定义数值文本
  （默认仍是百分比，悬停系数滑块传 `"0.8"` 这种一位小数）。

### 变更

- **默认系数 `0.5` → `0.8`**：鼠标压过去时提亮更明显。想要旧行为，把滑块拖到 `0.5` 即可。
- 滑块内部以 **0.1 为 1 档**（`HOVER_RATIO_UNITS = 10`）：复用既有的整数滑块机制，
  取值精度天然是 0.1，也**不会引入浮点步进误差**。
- 配置文件仍写 `hover_ratio`（浮点，保留 3 位小数），旧配置可直接沿用。

---

## 🧭 在此之前还补上了什么（v1.3.0 / v1.4.0 / v1.4.1）

仓库上一次发版停在 v1.2.0，所以这一版把中间三个版本一并带上：

| 版本 | 内容 |
| --- | --- |
| **v1.3.0** | 新增**「悬停半透明」**：鼠标压住的非焦点窗口平滑提亮。只提亮、绝不压暗 —— 全屏/聚焦/最大化/置顶窗口一律不参与。没有"鼠标进出任意窗口"的全局事件，改用 50ms 轮询 `GetCursorPos` + `WindowFromPoint`，**光标没动就直接短路** |
| **v1.4.0** | **最大化 / 置顶窗口视作聚焦**（跟随「聚焦最高透明度」）；**全屏窗口恒定 100%**（独立常量，不受最高值设置影响）。新增托盘开关「全屏窗口固定 100%」，`--list` 增加 `F` / `Z` / `T` 标记列 |
| **v1.4.1** | **修复「一切最大化就变 100%」**：根因是 Windows 最大化时会把窗口矩形**撑到屏幕外面**（那圈看不见的缩放边框），实测 `(-8,-8,1928,1088)` 对 `(0,0,1920,1080)` —— 只按"矩形是否盖住屏幕"判，**所有最大化窗口都会被误判成全屏**。修法是 `IsZoomed()` **一票否决** + 矩形判定留 2px 内缩容差 |

判定优先级链现在是：

```
全屏（恒 100%，不受「聚焦最高透明度」影响）
  └─ 聚焦（前台窗口）           → 聚焦最高透明度
      └─ 最大化 IsZoomed()      → 聚焦最高透明度
          └─ 置顶 TOPMOST       → 聚焦最高透明度
              └─ 悬停（鼠标压住） → 最低 + (最高 − 最低) × 滑块值
                  └─ 未聚焦      → 非聚焦最低透明度
```

---

## ✅ 验证结果

| 测试 | 内容 | 结果 |
| --- | --- | --- |
| `slider_test.py` | 取值量化与夹紧 / x→档位映射 / **离屏真画并用 `GetPixel` 反查** / 配置往返与节流 / 滚轮与点击字段 / 数值输入框 / 菜单项逐项核对 / **悬停系数档位↔系数双向换算** / **最大化≠全屏回归** | **254 PASS / 0 FAIL** |
| `hover_live_test.py` | **真实桌面 + 真实光标**上的悬停判定与状态判定链（全屏 > 聚焦 > 最大化 > 置顶 > 悬停 > 未聚焦），并证明整轮**一个写接口都不调** | **19 PASS / 0 FAIL / 2 SKIP** |
| `robustness_test.py` | 参数边界 / 零窗口 / 30s 长跑内存与 CPU / 强杀还原 / 托盘 / 优雅退出 | **27 PASS / 0 FAIL** |
| `e2e_test.py` | 真实窗口聚焦 ↔ 失焦切换，实测透明度 100% ↔ 40% | **PASS** |
| `install_test.py` | 沙盒完整走 安装 → 校验 → 卸载 → 清理（用本版安装包实测） | **25 PASS / 0 FAIL** |
| `fs_probe.py` | 真窗口三态取证：普通 / 最大化 / 真全屏，摊开 `IsZoomed` + 窗口矩形 + `rcMonitor` + `rcWork` | **全部符合预期** |
| `fs_e2e.py` | 让**打包好的 exe** 去给真实窗口分类（标记链 `--- → -Z- → F-- → ---`，目标 `40% → 80% → 100% → 40%`） | **打包产物判定正确** |

本版关键实测（`slider_test.py` 新增断言）：

```
滑块档位 8  → 显示 "0.8" → 系数 0.8      档位 0 → "0.0"     档位 10 → "1.0"
拖到档位 3  → 系数 0.3 → 悬停值 58%     ← 系数与悬停值同步变化
拖到档位 10 → 系数 1.0 → 悬停值 100%
系数 0.0 → 50%   0.1 → 55%   …   0.9 → 95%   1.0 → 100%      (最低 50 / 最高 100)
```

`hover_live_test.py` 的隔离设计：动画（`_tick`）只在**假 HWND** 上跑
（`IsWindow()` 返回 false → `_apply_alpha` 提前退出），并由一个写接口计数器兜底断言 ——
整轮下来 `SetLayeredWindowAttributes` / `SetWindowLongPtr` 的调用次数必须为 **0**，
所以这些验证**不会动到你桌面上任何一个真实窗口**。

> 排障与实现细节见：
> [`docs/win_glass_v1.4.1_maximized_not_fullscreen_fix.md`](docs/win_glass_v1.4.1_maximized_not_fullscreen_fix.md)（最大化误判全屏的完整根因）、
> [`docs/win_glass_v1.3.0_hover_report.md`](docs/win_glass_v1.3.0_hover_report.md)（悬停的实现与"只提亮不压暗"）。

---

## 📋 环境要求

| 项 | 要求 |
| --- | --- |
| 系统 | Windows 10 / 11（**x64**） |
| Python | **不需要**，安装包已含运行时 |
| 管理员权限 | **不需要**（装到用户目录） |
| 其他依赖 | 无（程序只用 Windows 自带接口，纯 `ctypes`） |

---

## ⚠️ 已知限制

- 只支持 Windows；核心依赖 `WS_EX_LAYERED`，其他系统没有对应机制
- 部分**独占全屏**或**硬件加速自绘**的程序（某些游戏、播放器）不响应整窗透明度，属系统限制
- **全屏窗口默认接管并锁在 100%**；若某些全屏程序不喜欢被接管，用 `--skip-fullscreen`
  让它们完全不参与（程序会原样保留其透明度与扩展样式）
- 部分 **Electron / Chromium** 应用自带分层透明度，默认会**记录其原值并在退出时还原**；
  也可用 `--skip-foreign-layered` 直接跳过它们
- 若在用 **StartAllBack / ExplorerPatcher / Windhawk** 等外壳增强工具，它们会挂钩菜单代码，
  可能干扰自绘菜单项（滑块拖不动时可先在干净系统上验证）
- 装到用户目录，因此**只对当前用户生效**；多用户环境需各自安装
