# 更新日志

本文件记录每个版本的变更。版本号遵循 `主版本.次版本.修订号`。

---

## v1.7.0 — 托盘菜单：右键绑全局快捷键 / 13 种界面语言 / Win11 圆角

### 新增

- **右键菜单项录制全局快捷键**：右键任意一个可绑定项（暂停、立即恢复、悬停、全屏、
  层叠衰减、渐隐时间、打开日志、退出）即进入录制，按下单键或组合键并松开即绑定并
  **自动保存**；右键空白处 / Esc 取消；录制时单按 Delete / Backspace 解绑。
  - 用 `RegisterHotKey` 注册，热键 id 直接复用命令 ID，收到 `WM_HOTKEY` 即分发，无需查表。
  - 录制期间用 `WH_KEYBOARD_LL` 低级别键盘钩子**吞掉所有按键**，避免被菜单的模态循环
    当成助记符/方向键吃掉；菜单关闭（含 `_popup` 的 `finally`）一定会卸载钩子，杜绝系统级键盘失灵。
  - 状态机：keydown 记下 `(修饰键, 主键)` 候选（修饰键用 `GetAsyncKeyState` 读**物理状态**，
    不吃"录制前就按住 Ctrl"的亏），keyup 才最终确认。
- **快捷键冲突 / 无效键保护**：
  - `is_bindable_vk` 拒绝修饰键 / Esc / 鼠标键作为主键；
  - 绑定前用 `owner_of_combo` 查重，重复则提示「与 xx 冲突」；
  - 真正 `RegisterHotKey` 失败（被系统/其它程序占用）才提示「注册失败」，绝不写进配置。
- **每项两行显示**：标题 + 灰色小字——已绑显示组合（附「右键解绑」），未绑显示
  「右键设置快捷键」；冲突/失败一次性提示用强调色画。菜单项改为 owner-draw 两行。
- **界面语言子菜单**：内置 **13 种**语言（`auto` / 简中 / 繁中 / 英 / 日 / 韩 / 德 / 法 /
  俄 / 阿 / 葡 / 西 / 意）。`auto` 经 `GetUserDefaultUILanguage` 解析成具体语言；
  文案走 `{占位符}` 三级回退（当前 → en_US → key 名）以支持词序差异。选中即生效并落盘。
- **Win11 圆角菜单**：菜单窗口套 `DWMWA_WINDOW_CORNER_PREFERENCE=ROUND`，边框与选中
  高亮块走 `RoundRect` 圆角；Win10（build < 22000）自动方角，无害。

### 变更

- 配置新增两个键：`language`（`str`，默认 `auto`）、`shortcuts`（`{str cid: "Ctrl+Alt+P"}`）。
  旧配置无这两个键时沿用默认，无需迁移。
- 托盘菜单项全部改为 owner-draw 两行；开关项的勾选标记改为手绘「✓」（不再依赖系统 `MFS_CHECKED`）。
- 新增命令行参数：`--lang <code>`、`--list-langs`、`--reset-shortcuts`。

### 实现要点（为什么要这么写）

- 快捷键存 `shortcuts = {int cid: "Ctrl+Alt+P"}`，落盘时键转 `str`（JSON 要求）。
  `parse_combo` / `format_combo` 互为逆运算，读回即归一化；`_sanitize_shortcuts` 洗掉手改/
  坏数据，绝不因一条坏快捷键让整个程序起不来。
- i18n 拆成独立纯模块 `win_glass_i18n.py`（无 ctypes、可单测），`GlassConfig` 只持有一个
  `I18N` 实例，切语言只换实例不传参。
- 新增单测 `i18n_shortcut_test.py`：组合往返、可绑定性、冲突检测、洗数据、三级回退、持久化。

---

## v1.6.0 — 层叠衰减：未聚焦窗口按堆叠顺序逐层递减

### 新增

- **层叠衰减**：所有**普通未聚焦窗口**按 Windows 的 **Z 序**（最顶 → 最底）依次编号，
  第 1 层用「非聚焦最低透明度」，之后每一层乘一次「层衰减系数」：

  ```
  第 1 层 = base
  第 n 层 = 四舍五入( 第 n−1 层已经显示的整数 × ratio )，但不低于 5%
  ```

  默认 `base=40%` / `ratio=0.70` ⇒ `40 / 28 / 20 / 14 / 10 / 7 / 5 / 5 …`
  把非聚焦滑块拖到 50% 就是用户给的原例：**聚焦 90% / 50% / 35% / 25%**。
  解决的问题是：以前 4 个未聚焦窗口全是同一个 40%，彼此**没有任何层次**，
  看不出谁压在谁上面，视觉上会糊成一片。
- **第四個滑块「层衰减系数」**：范围 **0.1 ~ 1.0**、步进 **0.1**、显示**一位小数**，
  与悬停系数滑块复用同一套「整数档位 × 0.1」机制，不引入浮点步进误差。
- **菜单里的「层叠衰减」开关项**：菜单上直接显示第 1~4 层的当前计算值
  （如 `层叠衰减（第 1~4 层 40/28/20/14%）`），点一下整体开/关。
- 新增命令行参数 `--no-layer-decay`（关）与 `--layer-decay-ratio <0.1~1.0>`。
- 新增 `--list` 体检的**「层」列**：每个窗口显示它排第几层（`0` = 不参与层叠），
  并多打一行「层叠衰减：开　第 1~5 层 = …」。
- 新增纯函数 `round_half_up()` / `layer_pct()` / `z_order_windows()` / `assign_depths()`，
  便于单测与 `--list` 复用同一份逻辑。

### 变更

- 配置文件新增两个键：`layer_decay`（布尔，默认 `true`）、
  `layer_decay_ratio`（浮点，默认 `0.70`，保留 3 位小数）。旧配置直接沿用，无需迁移。
- 「未聚焦」这一档由"单一值"细化为"按层递减链"，优先级链末端从
  `未聚焦 → 非聚焦最低透明度` 改为 `普通未聚焦 → 层叠衰减`。

### 实现要点（为什么要这么写）

- **四舍五入必须是四舍五入**：Python 内建 `round()` 是**银行家舍入**（`round(24.5) == 24`），
  而本功能明确要求 24.5 → 25，否则用户按 `50 → 35 → 25` 心算对不上，所以自己实现了 `round_half_up()`。
- **用「上一层已取整的显示值」递推，而不是 `base × ratio^(n−1)`**：
  这样每一层的数字都能拿屏幕上看到的上一层直接心算验证（`25 × 0.7 = 17.5 → 18`）。
  代价是舍入误差顺层累积——这是**有意**的取舍：链内自洽、肉眼可验，比"绝对精确但和屏幕对不上"更实用。
- **全屏 / 聚焦 / 最大化 / 置顶不占层级号**：它们各有更高优先级，既不参与递减，
  也不把后面的窗口挤下一层。尤其是**置顶窗口**——它物理上位置很靠上，
  若让它占号会把它下面所有窗口整体推低一层，而它自己又不参与递减 ⇒ 层级无法解释。
- **`_update_targets()` 改成三趟**：① 判定全部状态并收集「普通未聚焦」集合
  ② 按 Z 序给集合编号 ③ 逐个算目标值。必须分三趟，因为先算的窗口
  不知道后面还有几个普通未聚焦窗口排在它下面。三趟全是**只读**，不写 alpha。
- **`GetTopWindow` / `GetWindow` 必须显式声明 argtypes**：不声明时 64 位 HWND
  会**被静默截断**（和之前 `IsZoomed` 踩的是同一类坑），返回看似正常实则错误的值。

### 验证

- `slider_test.py`：**302 项全通过**。新增 K 段共 46 项断言覆盖 ——
  `round_half_up` 边界（`24.5→25`、`17.5→18`、`2.5→3`）、递推链 `50/35/25/18/13/9/6/5`、
  用户原例 `90/50/35/25`、5% 封底（含 `ratio=0.1` 极限）、`ratio=1.0` 退化为不衰减、
  `assign_depths` 只给普通未聚焦编号且随 Z 序反转、`target_for` 层叠分支、
  **置顶/最大化/全屏/聚焦带 depth 时仍走各自更高优先级**、系数夹紧 `[0.1,1.0]`、
  config.json 往返、CLI `--no-layer-decay` / `--layer-decay-ratio`。
- `hover_live_test.py`：**19 项全通过 / 0 失败**（真实桌面 + 真实光标，写接口调用次数为 0）。
- `menu_e2e_test.py`：**20 项全通过**（真弹菜单 + 合成鼠标拖动 + 截图核对，滑块数量改为 4）。
- `robustness_test.py`：**27 项全通过**（强杀还原 / 优雅退出 / 长跑）。
- `e2e_test.py`：**全通过**（真实焦点切换，100% ↔ 40%）。
- `install_test.py`：**25 项全通过**（沙盒安装 → 校验 → 卸载 → 清理）。
- 三个 exe 的版本资源均确认为 `1.6.0.0`，`CompanyName` 为 `Naiqey.千鵺`。

### 顺手修掉的测试自身问题（非产品 bug）

- `slider_test.py`：3 处"滑块数量 == 3"的过期断言（上一版加第 3 个滑块时漏改）；
  `NumberInputBox` 偶发失败的真因是**驱动线程可能在子控件创建前就抓到顶层窗口**，
  改为轮询等待编辑框/按钮到位后不再抖动（连跑 3 次 302/302）。
- `menu_e2e_test.py`：断言从硬编码 `2` 改为比对**完整标签列表**，下次再加滑块不会又假失败。
- `hover_live_test.py`：两处过期假设 ——（a）"其余未聚焦窗口都是 40%"在层叠衰减开启后
  不再成立，改为显式关掉层叠以隔离悬停语义；（b）`HOVER = 70` 注释写着"中点"，
  但 v1.5.0 把系数改成可调后默认是 `0.8`（⇒ 88%），改为显式钉住 `hover_ratio = 0.5`。

---

## v1.5.0 — 悬停插值系数改为可调滑块（0.0 ~ 1.0，步进 0.1）

### 新增

- **第三个滑块「悬停插值系数」**：托盘右键菜单里直接拖，范围 **0.0 ~ 1.0**，
  **步进 0.1**，显示**一位小数**（`0.0` / `0.5` / `0.8` …）。
- 悬停透明度**不再用写死的系数**，改为跟随滑块实时计算：

  ```
  悬停值 = 透明度最小值 + (最大值 − 最小值) × 滑块值
  ```

  拖动它，菜单上面那行「悬停半透明（未聚焦窗口 → xx%）」会**实时跟着变**。
- 新增两个显示格式化函数 `fmt_pct()` / `fmt_ratio_units()`；
  owner-draw 绘制函数 `draw_menu_slider()` 支持传入自定义数值文本
  （默认仍是百分比，悬停系数滑块传 `"0.8"` 这种一位小数）。

### 变更

- **默认系数 `0.5` → `0.8`**。`0.8` 偏向"更接近清晰"那一侧，
  鼠标压过去的窗口提亮更明显；想要旧行为把滑块拖到 `0.5` 即可。
- 滑块内部以 **0.1 为 1 档**（`HOVER_RATIO_UNITS = 10`）：复用既有的整数滑块机制，
  取值精度天然是 0.1，也**不会引入浮点步进误差**。
- 配置文件仍写 `hover_ratio`（浮点，保留 3 位小数），旧配置可直接沿用。

### 验证

- `slider_test.py`：**254 项全通过**，其中新增断言覆盖 —— 三个滑块的 ID/范围/顺序、
  档位 ↔ 系数双向换算（8 → `0.8`、0 → `0.0`、10 → `1.0`）、
  拖动后**系数与悬停值同步变化**（档位 3 → 0.3 → 58%）、越界夹紧、落盘读回。
- `hover_live_test.py`：**19 项全通过**（真实桌面 + 真实光标，写接口调用次数为 0）。

---

## v1.4.1 — 修复：最大化窗口被误判为「全屏」

### 修复

- **现象**：任何窗口一旦最大化，透明度就变成 100%，用户设的「聚焦最高透明度」失效。
- **根因**：全屏判定原来只做**零容差的「窗口矩形是否包含屏幕」**判断，
  而 **Windows 最大化时会让窗口矩形越过屏幕边缘**（那圈看不见的缩放边框
  `SM_CXSIZEFRAME` + `SM_CXPADDEDBORDER`，本机 = 4 + 4）。实测：

  ```
  最大化窗口矩形 = (-8, -8, 1928, 1088)      屏幕 = (0, 0, 1920, 1080)
      rc.left(-8)  <= 0        ✓
      rc.right(1928) >= 1920   ✓      ← 越出反而"帮助"通过了包含判定
      ⇒ 包含成立 ⇒ 最大化被判成全屏 ⇒ 锁定 100%
  ```

  **越出屏幕不是异常，而是破坏力的来源** —— 它让"覆盖屏幕"这个条件更容易成立。
- **加重因素**：任务栏设为**自动隐藏**时 `rcWork` 与 `rcMonitor` 完全相等，
  所以不能用"最大化只覆盖 rcWork"来区分两者。
- **修复**：`IsZoomed()` **一票否决** —— 用户按了最大化的窗口一律不算全屏
  （真全屏走的是"把矩形撑满"，不设 `SW_MAXIMIZE`，`IsZoomed()` 为 `False`）；
  另把矩形判定抽成纯函数 `rect_covers_monitor()` 并留 **2px 内缩容差**
  （不同 DPI 缩放下真全屏窗口会差一两像素）。

### 新增

- `fs_probe.py`：真窗口三态取证脚本，把 `IsZoomed` / `GetWindowRect` /
  `rcMonitor` / `rcWork` 四行摊开对比，一眼看清根因。
- `fs_e2e.py`：让**打包好的 exe** 去给真实窗口分类，端到端验证判定链。

### 验证

- `slider_test.py`：**231 项全通过**（新增 16 项针对"最大化 ≠ 全屏"的回归断言，
  含"判否时一次 `MonitorFromWindow` 都不调"的短路证明）。
- `hover_live_test.py`：**24 项全通过**（新增 F/Z 标记互斥不变量）。

---

## v1.4.0 — 最大化 / 置顶视作聚焦；全屏恒定 100%

### 新增

- **全屏窗口恒定 100%**：新增独立常量 `FULLSCREEN_ALPHA = 1.0`，
  **不复用**「聚焦最高透明度」—— 把最高值调到 60% 是为了看清背景窗口，
  不是为了把全屏的视频压暗。
- **托盘菜单新增「全屏窗口固定 100%」开关**（`CMD_FULLSCREEN`，默认勾选）：
  关掉后全屏窗口**完全不接管**（原样保留，给全屏游戏留退路）。
- `read_window_state()` 统一读出「置顶 / 最大化 / 全屏」三态；
  `--list` 输出新增 `F` / `Z` / `T` 标记列与「全屏固定=100%」声明。

### 变更

- **判定优先级链**明确为：

  ```
  全屏(恒 100%) > 聚焦 > 最大化 > 置顶 > 悬停 > 未聚焦
  ```

  即 **最大化窗口与置顶窗口同样视为聚焦**，遵循已配置的「聚焦最高透明度」。
- `target_for()` 收成**强制关键字参数**：`is_fg` / `top` / `zoomed` / `fullscreen`
  语义相近，位置传参写反了**看不出错**（都会落到同一个值），宁可让它直接 `TypeError`。
- `--skip-fullscreen` 语义明确为"完全不接管全屏"；
  旧的 `--no-skip-fullscreen` 保留为**无操作参数**以兼容旧脚本。
- 全屏 100% 走**快捷路径、不额外加 `WS_EX_LAYERED`**：本来不透明的全屏窗口要设成
  100%，正确做法是什么都不做 —— 一旦加上分层位，DWM 的独立翻转 / 硬件叠加优化
  会被关掉，全屏视频和游戏可能掉帧。

### 验证

- `slider_test.py`：**215 项全通过**。
- `hover_live_test.py`：**22 项全通过**，`SetLayeredWindowAttributes` /
  `SetWindowLongPtr` 调用次数均为 **0**（判定层验证零副作用）。
- `--self-test` 在打包产物上实测：最高值压到 80% 时，聚焦/最大化/置顶段读回
  **204（=80%）**，而全屏段读回 **255（=100%）** 且不随 80% 变动。

---

## v1.3.0 — 新增「悬停半透明」：鼠标压住的窗口提亮

### 新增

- **悬停提亮**：鼠标停在一个**非焦点**窗口上时，该窗口从「非聚焦最低透明度」
  平滑提亮到插值点（当时默认系数 `0.5`，即取中间值）。
- **托盘菜单新增「悬停半透明」开关**（`CMD_HOVER`，默认勾选）。
- 命令行：`--no-hover`（关掉）、`--hover-ratio`（插值系数）、
  `--hover-interval`（光标轮询间隔，默认 0.05s）。
- `hover_live_test.py`：在**真实桌面 + 真实光标**上验证悬停判定，
  且证明整轮下来一个写接口都不调（`SetLayeredWindowAttributes` 调用次数 = 0）。

### 修复 / 设计要点

- **只提亮、绝不压暗**：参与者只有「本应被压暗的未聚焦窗口」。
  全屏 / 聚焦 / 最大化 / 置顶当前都是最高透明度，按同一个系数算只会把它们压暗 ——
  这不是用户按着鼠标想要的，所以一律不参与。
  另有 `max(最低值, 插值结果)` 兜底：反向配置（最高 < 最低）下退化为"不改变"。
- **没有全局的"鼠标进入/离开任意窗口"事件**，所以只能轮询：
  `GetCursorPos` + `WindowFromPoint` + `GetAncestor(GA_ROOT)`，
  50ms 一次、**光标没动就直接短路**（连 API 都不调）；变化只置脏标记，重算交给主循环。
- **快速切换悬停目标不跳变**：新目标从**当前透明度**起缓动（复用既有的 `src = cur`），
  所以鼠标在多个窗口之间划过时是一条连续曲线上的折线。

### 验证

- `slider_test.py`：**191 项全通过**。
- `hover_live_test.py`：**19 项通过 / 1 跳过**（跳过项为"光标此刻不在受管窗口上"，环境相关）。
- `--self-test` 实测缓动序列：`255 → 128(50%) → 191(75%) → 128 → 255`，
  悬停段正好落在 75%。

---

## v1.2.0 — 滑块改为音量条样式，新增「渐隐时间」调节

### 新增

- **渐隐时间可调**：托盘右键菜单新增「**渐隐时间…**」项，点开弹出一个数值输入框，
  直接填毫秒数（**1 ~ 5000 ms**，默认 500）。改完**立即生效** —— 正在跑的过渡会按新时长
  重排时间轴（已完成进度保持不变），不用等下一次切换。
  - 输入越界会自动夹紧到合法范围；填非数字则退回原值；点「取消」/ 直接关窗什么都不做。
  - 该值同样写入 `config.json`（键 `fade_ms`），下次启动自动生效。
  - 命令行 `--fade-ms` 同步支持持久化，优先级仍是 **命令行显式值 > 配置文件 > 内置默认**。

### 修复

- **修复「鼠标停在滑块上拖动时，数值和进度条不刷新」的问题（本次核心根因）**
  `WH_MSGFILTER`(`MSGF_MENU`) 回调里的 `msg.hwnd` **不一定就是菜单窗口** ——
  钩子看到的第一条消息，`hwnd` 是 shell 的 `SystemUserAdapterWindowClass`（一个无关的
  辅助窗口）。旧代码把这个句柄**无条件缓存**成了菜单位置，于是后续所有
  `InvalidateRect` / `RedrawWindow` 都打在无关窗口上：**不报任何错**，但菜单永远不会重绘。
  表现就是「拖动时数值和进度条怎么都不动，指针一移出去才刷新」（指针离开会触发菜单
  自己的高亮重绘，所以看起来像"补上了"）。
  修法：只接受窗口类为 `#32768` 的句柄；新增 `_resolve_menu_hwnd()` 逐级校验
  （缓存 → `FindWindowW("#32768")` → 按菜单项中心点 `WindowFromPoint`），
  并把重绘换成 `RedrawWindow(..., RDW_INVALIDATE | RDW_UPDATENOW)` 强制同步重画。
  回归测试见 `menu_live_test.py`（把 `_redraw_item` 换成空函数即可复现旧症状）。

### 变更

- **滑块外观与交互对齐 Windows 任务栏音量条**：由原来的「分段式细格条（每格 1%）」
  改为 **细轨道 + 圆形手柄**。
  - 轨道 4px 细条，已填充部分从最左端一直接到手柄圆心，手柄为圆形。
  - 手柄在**悬停 / 拖动 / 方向键微调**时放大一圈（半径 7px → 9px），有明确的跟随反馈。
  - 颜色跟随**系统强调色**（读 `HKCU\...\DWM\AccentColor`，取不到再退到
    `Explorer\Accent\AccentColorMenu` / `DwmGetColorizationColor` / 默认蓝），
    深色主题下自动换用对比色；选中行（强调色背景）单独一套配色。
  - 手柄行程两端各内缩 `SLIDER_THUMB_INSET`，滑到 0/100 时手柄不会被菜单项边缘切掉。
  - **取值精度不变**：仍是 1% 步进、整数显示，三种调法（拖动 / 滚轮 / 方向键）全部保留。

- 菜单结构：暂停 / 立即恢复 / 非聚焦最低透明度 / 聚焦最高透明度 / **渐隐时间…** / 打开日志 / 退出
- `--list` 输出增加 `渐隐=<n>ms`，方便在命令行核对当前渐隐设置
- `build.py` 的版本号改为**单一来源**：`filevers` / `FileVersion` / `ProductVersion`
  全部由 `VER` 常量派生，不再手抄三份（以前漏改一处，exe 属性里就是旧版本）

### 验证

| 测试 | 内容 | 结果 |
| --- | --- | --- |
| `slider_test.py` | 取值量化与夹紧（含渐隐 1~5000ms） / x→百分比映射（1% 步进、两端可达） / **真画一遍再用 `GetPixel` 反查「填充是否止于手柄」「手柄是否为圆」「悬停是否放大」** / 配置往返与节流 / 滚轮与点击字段 / **数值输入框端到端（含夹紧、取消、关窗不杀进程）** / **菜单项逐项核对（含渐隐项 ID 与显示值）** | **153 PASS / 0 FAIL** |
| `menu_live_test.py` | **指针停在菜单项上拖动时，数值文字与进度条是否实时重绘**（BitBlt 取像素差异判定） + 菜单不关闭 + 点外部关闭 | **7 PASS / 0 FAIL** |
| `menu_e2e_test.py` | 真弹菜单并截图 / 合成鼠标拖动（菜单不关、值实时变） / 方向键微调 / 落盘整数 / 点外部正常关闭 | **18 PASS / 0 FAIL** |
| `robustness_test.py` | 参数边界 / 零窗口 / 30s 长跑 / 强杀还原 / 托盘 / 优雅退出 | **27 PASS / 0 FAIL** |
| `e2e_test.py` | 真实焦点切换，透明度 100% ↔ 40% | **PASS** |

> 测试里两处期望值原先写死成 `40%/100%`（`e2e_test.py`、`slider_test.py` 的 CLI 段），
> 用户一旦自己拖过滑块就会假失败。本版改为**从配置文件/命令行实参推导期望值**；
> `e2e_test.py` 与 `robustness_test.py` 也统一加上了 `--no-config`，
> 保证测试**不会写坏用户真实的 `config.json`**。

---

## v1.1.0 — 托盘右键菜单新增两个分段式透明度滑块

### 新增

- **托盘右键菜单里可以直接调透明度了**，两个滑块：

  | 滑块 | 作用 | 范围 | 默认 |
  | --- | --- | --- | --- |
  | 非聚焦最低透明度 | 没在用的窗口淡化到多少 | **5% ~ 95%** | 40% |
  | 聚焦最高透明度 | 正在用的窗口清晰到多少 | **5% ~ 100%** | 100% |

  - **分段式显示**：一格 = 1%，点亮几格就是百分之几
  - **步进恒为 1%，取值恒为整数**，不会出现小数点
  - **三种调法**：按住拖动 / 鼠标滚轮 ±1% / 左右方向键 ±1%
  - 拖动过程中菜单**不会关闭**，松手后继续操作别的菜单项

- **取值持久化**：两个滑块的值写入 `%LOCALAPPDATA%\win_glass\config.json`，下次启动自动生效。
  写盘做了 0.3s 节流，拖动时不会疯狂刷盘；关菜单 / 退出 / `--save-config` 时强制落盘。

- 新增命令行参数：`--active-alpha`（聚焦窗口透明度）、`--no-config`（既不读也不写配置，
  滑块改动只在本次运行有效）、`--save-config`（把本次参数写入配置后继续运行）。
  优先级：**命令行显式值 > 配置文件 > 内置默认**。

### 修复

- **修复滑块尺寸不生效、菜单项一片空白的问题（本次的核心根因）**
  `WM_MEASUREITEM` 与 `WM_DRAWITEM` 的消息号写反了（正确值：`WM_DRAWITEM=0x2B`、
  `WM_MEASUREITEM=0x2C`，源码里写成了 `0x2B=MEASUREITEM`）。
  写反**不会报任何错、消息也照样收得到**，只是「拿到的是另一个结构体」：
  宽高被写进 `DRAWITEMSTRUCT` 的 `itemAction/itemState`，绘制时读到的 `rcItem`
  又是 `MEASUREITEMSTRUCT` 里的残留值 → 表现就是「怎么设都是默认的 203×16、整项空白」。
  定位过程：`probe_map.py` 打印「消息号 ↔ 结构体形状」对照，
  `probe_raw.py` 把两个结构体的原始内存逐字节 dump 出来核对偏移。

- **修复滚轮读了错误字段**
  滚轮增量在 `lParam` 的**高 16 位**；原先从 `wParam` 高位取，取到的是光标 y 坐标，
  表现为「滚轮乱跳或完全不动」。回归测试见 `slider_test.py` 的 E 段。

- **`MFT_OWNERDRAW` 常量用错值会静默失败**
  正确的是 `0x00000100`，写成 `0x00001000` 时 `InsertMenuItemW` 直接失败并返回
  `ERROR_INVALID_PARAMETER(87)`，菜单里不会出现滑块（现在失败会打印明确警告）。
- 诊断用的消息明细原先会无限增长，改为最多保留 64 条，常驻进程不会越跑越占内存。

### 变更

- 菜单结构：暂停 / 立即恢复 / **非聚焦最低透明度** / **聚焦最高透明度** / 打开日志 / 退出
- 启动日志与 `--version` 输出带上当前滑块取值（均为整数百分比）

### 验证

| 测试 | 内容 | 结果 |
| --- | --- | --- |
| `slider_test.py` | 取值量化与夹紧 / x→百分比映射（1% 步进、两端可达） / **真画一遍再用 `GetPixel` 反查填充边界** / 配置往返与节流 / 滚轮与点击的字段解析 | **79 PASS / 0 FAIL** |
| `menu_e2e_test.py` | 真弹菜单并截图 / 合成鼠标拖动（菜单不关、值实时变） / 方向键微调 / 落盘整数 / 点外部正常关闭 | **18 PASS / 0 FAIL** |
| `robustness_test.py` | 参数边界 / 零窗口 / 30s 长跑 / 强杀还原 / 托盘 / 优雅退出 | **27 PASS / 0 FAIL** |
| `e2e_test.py` | 真实焦点切换，透明度 100% ↔ 40% | **PASS** |

---

## v1.0.1 — 修复安装后不可用，改为无窗口 + 系统托盘

### 修复

- **修复「点开始菜单快捷方式弹出记事本」的问题**
  v1.0.0 用一个 `.vbs` 脚本当最小化启动器，而在 `.vbs` 关联被改成记事本的机器上，
  双击快捷方式只会「用记事本打开脚本」，程序根本没启动。
  → v1.0.1 **彻底删掉 `.vbs` 启动器**，快捷方式**直指 `win_glass.exe`**；
  升级安装时还会主动删除残留的 `win_glass.vbs`，自动修复坏掉的旧快捷方式。

- **修复「启动后弹一个命令行黑窗口」的问题**
  主程序从 `--console` 构建改为 **`--windowed`（完全无窗口）**，日常交互改到系统托盘上。
  另外单独提供一份 `win_glass-console.exe` 用于需要看输出的诊断场景。

- **修复托盘「退出」时不还原窗口的问题**
  `shutdown()` 原先用 `_stop` 判断幂等，而托盘退出正是先置 `_stop`，
  导致所有窗口卡在 40% 透明度不还原。改用独立的 `_shutdown_done` 守卫。

- **修复卸载时程序收不到退出信号的问题**
  托盘窗口原本建成消息窗口（`HWND_MESSAGE`），卸载器用 `taskkill`（不带 `/F`）发的
  WM_CLOSE 它收不到，会被降级成强杀，窗口就留在半透明状态。
  改用**隐藏的顶层窗口**，并新增 WM_CLOSE / 注销 / 关机 的还原处理。

- **修复 `--log` 参数失效的问题**
  窗口化模式下 `--log` 会被 import 期的默认日志顶掉，现在目标日志变更时会重开文件句柄。

### 变更

- 系统托盘交互：**左键单击** = 暂停 / 继续；**右键** = 暂停、立即恢复、打开日志、退出
- 托盘窗口带 `WS_EX_TOOLWINDOW`，**不进任务栏、不进 Alt+Tab**
- 版本号同步到安装包文件名、程序文件属性、`--version`、启动日志与「程序和功能」列表

### 验证

| 测试 | 结果 |
| --- | --- |
| `robustness_test.py` 健壮性（参数边界 / 零窗口 / 30s 长跑 / 强杀还原 / 托盘 / 优雅退出） | **27 PASS / 0 FAIL** |
| `e2e_test.py` 端到端（真实焦点切换，实测 100% ↔ 40%） | PASS |
| `install_test.py` 安装包沙盒（安装 → 校验 → 卸载 → 清理） | PASS |

---

## v1.0.0 — 首个版本

- 窗口聚焦透明度引擎：有焦点 / 置顶窗口 100%，其余 40%
- 500ms smoothstep 缓动过渡，不会生硬跳变
- 纯 `ctypes` 实现，不依赖 `pywin32`
- 窗口枚举与过滤（桌面、任务栏、输入法、提示框、全屏窗口等）
- 退出时还原透明度与窗口扩展样式
- 命令行参数支持，附端到端与健壮性测试
